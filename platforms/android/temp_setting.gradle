android {
            defaultConfig {
                applicationId = "org.nativescript.CryptoTracker"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }